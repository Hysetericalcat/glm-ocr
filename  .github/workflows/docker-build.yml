name: Build and Push Docker Image

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: akshatjan
          password: Akshat@3624

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags:  akshatjan/glm-ocr:latests